"""OCR assist: per-page quality scoring, retry loop, AI diagnostician + vision.

This package is the home for the interactive OCR workflow (see
``docs/planning/INTERACTIVE_OCR_PLAN.md``). Provider-agnostic contracts live
in ``contracts.py``; LLM implementations live under ``providers/``.
"""
