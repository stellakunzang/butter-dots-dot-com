# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# File handling
python-multipart==0.0.6

# Tibetan text processing
regex==2023.12.25
striprtf==0.0.29

# --- PDF / OCR pipeline ---

# Digital PDF text extraction
pdfplumber==0.11.0

# PDF → image conversion (for scanned PDFs)
pdf2image==1.17.0

# BDRC OCR pipeline dependencies (vendored from buda-base/tibetan-ocr-app, MIT)
onnxruntime==1.17.1
opencv-python-headless==4.9.0.80
numpy==1.26.4
scipy==1.12.0
pyewts==0.2.0
pypdf==4.1.0

# PDF annotation (annotated PDF output)
pymupdf==1.24.1

# Editable .docx output (translator-facing)
python-docx==1.1.0

# Pillow (image handling)
Pillow==10.2.0

# HTTP client / testing
httpx==0.26.0
pytest==7.4.4
pytest-asyncio==0.23.3
