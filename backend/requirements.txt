# Web framework
# FastAPI 0.115+ ships Starlette that supports httpx>=0.28 (needed for google-genai / T-15)
fastapi==0.115.6
uvicorn[standard]==0.27.0

# Data validation
# pydantic>=2.9 required by google-genai (T-15)
pydantic==2.10.6
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# File handling
python-multipart==0.0.6

# Tibetan text processing
regex==2023.12.25
striprtf==0.0.29

# --- PDF / OCR pipeline ---

# Digital PDF text extraction
pdfplumber==0.11.0

# PDF → image conversion (for scanned PDFs)
pdf2image==1.17.0

# BDRC OCR pipeline dependencies (vendored from buda-base/tibetan-ocr-app, MIT)
onnxruntime==1.17.1
opencv-python-headless==4.9.0.80
numpy==1.26.4
scipy==1.12.0
pyewts==0.2.0
pypdf==4.1.0
pyctcdecode==0.5.0
# Thin-plate-spline dewarping (imports as ``tps``); used when use_tps=True
thin-plate-spline==1.2.2

# PDF annotation (annotated PDF output)
pymupdf==1.24.1

# Editable .docx output (translator-facing)
python-docx==1.1.0

# Pillow (image handling)
Pillow==10.2.0

# HTTP client / testing
# httpx>=0.28.1 required by google-genai (T-15); FastAPI TestClient OK on 0.28.x
httpx==0.28.1
pytest==7.4.4
pytest-asyncio==0.23.3

# Claude API (interactive OCR diagnostician — T-06)
anthropic==0.105.2

# Gemini vision-OCR provider (T-07 / T-15) — used when GEMINI_API_KEY is set
google-genai==2.8.0
